# === 설치 경로 지정 (CPU 버전 torch를 위함) ===
--index-url https://download.pytorch.org/whl/cpu

# === 빌드 도구 (필수) ===
pyinstaller==6.14.2

# === 데이터 처리 및 구글 시트 연동 ===
pandas==2.2.2
gspread==6.2.1
oauth2client==4.1.3

# === 웹 API 호출 ===
requests==2.32.4
google-search-results==2.4.2

# === AI 모델 및 자연어 처리 ===
sentence-transformers==5.0.0
# 위에서 지정한 경로를 통해 CPU 전용 버전이 설치됨
torch==2.7.1
transformers==4.53.1
tokenizers==0.21.2
safetensors==0.5.3

# === GUI 그래프 및 핵심 의존성 (충돌 해결) ===
matplotlib==3.8.4
numpy==1.26.4
