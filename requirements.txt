# === 빌드 도구 (필수) ===
pyinstaller==6.14.2

# === 데이터 처리 및 구글 시트 연동 ===
# numpy 2.x 버전과의 충돌을 피하기 위해 안정적인 2.2.x 버전으로 지정
pandas==2.2.2

gspread==6.2.1
oauth2client==4.1.3

# === 웹 API 호출 ===
requests==2.32.4
google-search-results==2.4.2

# === AI 모델 및 자연어 처리 ===
sentence-transformers==5.0.0
torch==2.7.1
transformers==4.53.1
tokenizers==0.21.2
safetensors==0.5.3

# === GUI 그래프 및 핵심 의존성 (충돌 해결) ===
# Python 3.12 및 PyInstaller와 호환성이 검증된 안정 버전으로 수정
matplotlib==3.8.4
# matplotlib 3.8.x 버전과 가장 안정적으로 동작하는 numpy 1.26.x 버전 명시
numpy==1.26.4
